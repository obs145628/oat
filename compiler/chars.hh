#ifndef CHARS_HH_
# define CHARS_HH_

namespace chars
{
   bool isBlanck(char c);
   bool isVisible(char c);
   bool isValid(char c);
   bool isDigit(char c);
   bool isLower(char c);
   bool isUpper(char c);
   bool isLetter(char c);
   bool isSymbol(char c);
   bool isToken(char c);
}

#endif //!CHARS_HH_
