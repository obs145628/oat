import "maths.oat";
import "utils.oat";
import "lib2.oat";
import "Vector.oat";

myprint(value, key)
{
  print(key, " => ", value, "\n");
}

main(args)
{

  let x = promptBool("You are: ", true);

  kprint(x);

}
